{
  "name": "@epsilon-data/keycloak-admin-client",
  "version": "25.0.7",
  "main": "./index.js",
  "types": "./index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "esbuild index.ts --platform=node --bundle --format=cjs --outfile=index.js"
  },
  "dependencies": {
    "@keycloak/keycloak-admin-client": "^25.0.6"
  },
  "devDependencies": {
    "esbuild": "^0.18.17"
  }
}
